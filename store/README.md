# Store

This folder defines the different Vuex stores. Nuxtjs
automatically combines these into the main store using namespaces defined
by the name of the file. For example, user.js generates the
store.user-module.
