# Middleware

This folder contains the navigation middleware used in Nuxt.
For example the 'admin' middleware checks that the user
has an admin account. If they do not, they are not allowed
on the page.
